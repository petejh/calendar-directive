describe('CalendarDirectiveChallenge', function() {

  it("knows the truth when it sees it", function() {
    expect(true).toBeTruthy();
  });

  it("knows the truth when it doesn't", function() {
    expect(false).toBeTruthy();
  });

});
